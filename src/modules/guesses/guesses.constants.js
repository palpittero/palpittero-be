export const GUESS_POINTS_STRATEGY = {
  GROUPED: 'grouped', // 4 (four) closed rules that points a guess from 1 to 4
  ACCUMULATIVE: 'accumulative' // Endless individual rules that accumulate points for a guess
}
